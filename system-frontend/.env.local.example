# ก็อปไฟล์นี้เป็น .env.local แล้วแก้ URL ให้ตรงกับ backend จริง
# ทุกครั้งที่ย้ายโปรเจกต์ไปโฟลเดอร์/เครื่องใหม่ ต้องตรวจไฟล์นี้ก่อน (ไม่ถูก commit ขึ้น git)
# ต้องตรงกับค่า CORS_ALLOWED_ORIGINS และ SANCTUM_STATEFUL_DOMAINS ใน system-backend/.env ด้วย
NEXT_PUBLIC_API_URL=http://localhost:8000/api
